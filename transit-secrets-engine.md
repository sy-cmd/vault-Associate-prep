# Transit secrets engine 
+ the transit secrets engine is responsible for encrpting and decrypting data that is sent to it it doesn't store any data.
+ Transit engine it encrypts the data and stores it a storage store.
+ key derivation is supported , which is the process that allows the same key to generate new keys from it. 
+ Datakey generation allows processes to request for keys based on a specific key  