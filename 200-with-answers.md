# 🔐 HashiCorp Certified: Vault Associate (003) - 200 Practice Questions (No Answers)

> 📋 **Instructions**: Work through these questions without looking at answers. Track your responses, then check the answer key at the end when ready.

---

## 📊 Domain Distribution

| Domain | Weight | Questions |
|--------|--------|-----------|
| 1. Authentication Methods | 14% | 28 |
| 2. Vault Policies | 12% | 24 |
| 3. Vault Tokens | 12% | 24 |
| 4. Vault Leases | 8% | 16 |
| 5. Secrets Engines | 16% | 32 |
| 6. Encryption as a Service | 6% | 12 |
| 7. Architecture Fundamentals | 10% | 20 |
| 8. Deployment Architecture | 12% | 24 |
| 9. Access Management Architecture | 10% | 20 |

---

## 🔐 Domain 1: Authentication Methods (Questions 1-28)

### True/False
1. True or False: Vault tokens are generated by every authentication method login.

2. True or False: The `userpass` auth method requires an external identity provider.

3. True or False: AppRole authentication is designed primarily for human users.

4. True or False: Kubernetes auth method validates service account tokens against the Kubernetes API.

5. True or False: OIDC authentication requires manual entry of credentials each time.

### Multiple Choice
6. Which auth method is BEST suited for machine-to-machine authentication in a CI/CD pipeline?
   - A) Userpass
   - B) AppRole
   - C) GitHub
   - D) OIDC

7. What is the primary purpose of Vault identities and groups?
   - A) To encrypt stored secrets
   - B) To manage token TTLs
   - C) To map multiple auth aliases to a single logical entity
   - D) To rotate encryption keys

8. Which command authenticates to Vault using the CLI with the userpass method?
   - A) `vault auth login userpass`
   - B) `vault login -method=userpass username=admin`
   - C) `vault authenticate -user admin`
   - D) `vault auth enable userpass`

9. When would you choose LDAP auth over Token auth?
   - A) For automated scripts with static credentials
   - B) When integrating with existing corporate directory services
   - C) For temporary one-time access
   - D) When no network connectivity is available

10. Which statement about entity aliases is CORRECT?
    - A) An entity can only have one alias
    - B) Aliases link external auth identities to Vault entities
    - C) Aliases are used to encrypt tokens
    - D) Aliases replace the need for policies

### Multiple Answer (Choose 2)
11. Which are valid authentication methods in Vault? (Choose 2)
    - A) AppRole
    - B) SSH Key Exchange
    - C) Kubernetes
    - D) SMTP Auth
    - E) TLS Certificates

12. What capabilities does the Identity engine provide? (Choose 2)
    - A) Mapping multiple auth methods to a single user identity
    - B) Encrypting data at rest
    - C) Grouping entities for policy assignment
    - D) Automatically rotating secrets
    - E) Managing storage backends

13. Which statements about human vs system authentication are true? (Choose 2)
    - A) Human auth often uses interactive methods like OIDC
    - B) System auth requires manual password entry
    - C) System auth typically uses AppRole or Kubernetes methods
    - D) Human auth cannot use tokens
    - E) Both can be assigned policies

### Scenario-Based
14. Your application runs in Kubernetes and needs to access Vault secrets. Which auth method should you configure?
    - A) GitHub
    - B) AppRole
    - C) Kubernetes
    - D) Userpass

15. A legacy application requires a static credential for Vault access. Which approach is MOST appropriate?
    - A) OIDC with short TTL tokens
    - B) AppRole with wrapped secret ID
    - C) Token auth with a long-lived service token
    - D) LDAP with MFA

16. You need to authenticate developers using their corporate GitHub accounts. Which auth method should you enable?
    - A) AppRole
    - B) GitHub
    - C) Kubernetes
    - D) Cert

17. Which command enables the AppRole auth method at the default path?
    - A) `vault auth enable approle`
    - B) `vault secrets enable approle`
    - C) `vault auth add approle`
    - D) `vault enable auth/approle`

18. What does the `role_id` in AppRole authentication represent?
    - A) The user's LDAP username
    - B) A static identifier for the role
    - C) The token's accessor
    - D) The entity's group ID

19. When using response wrapping with AppRole, what is delivered to the application first?
    - A) The actual secret
    - B) A wrapping token containing the secret_id
    - C) The root token
    - D) The unseal key

20. Which auth method supports MFA natively in Vault Enterprise?
    - A) Token only
    - B) All auth methods with Enterprise MFA configuration
    - C) Only OIDC
    - D) Only LDAP

21. What is the purpose of `mount_accessor` in identity management?
    - A) To encrypt the auth method configuration
    - B) To uniquely identify an auth method mount for alias mapping
    - C) To set the TTL for tokens
    - D) To rotate authentication credentials

22. Which command lists all enabled auth methods?
    - A) `vault auth list`
    - B) `vault list auth/`
    - C) `vault secrets list auth`
    - D) `vault read sys/auth`

23. True or False: Disabling an auth method immediately revokes all tokens issued through that method.

24. Which parameter in `vault login` specifies the auth method path if it's not at the default location?
    - A) `--method-path`
    - B) `--path` or `-method=<path>`
    - C) `--auth-path`
    - D) `--mount`

25. What happens when an entity's policy is updated?
    - A) All existing tokens are immediately revoked
    - B) New tokens inherit the updated policy; existing tokens keep old policies until renewal
    - C) Policies only apply to new entities
    - D) The entity must re-authenticate immediately

26. Which auth method is enabled by default and cannot be disabled?
    - A) userpass
    - B) token
    - C) approle
    - D) kubernetes

27. For a microservice that needs to authenticate without human intervention, which combination is BEST?
    - A) OIDC + short TTL
    - B) AppRole + wrapped secret_id + auto-auth
    - C) Userpass + interactive login
    - D) GitHub + manual token generation

28. What does the `-field=token` flag do in `vault write auth/approle/role/myrole/secret-id`?
    - A) Sets the token policy
    - B) Returns only the secret_id value without JSON formatting
    - C) Encrypts the secret_id
    - D) Creates a wrapping token

<details>
<summary>Show Answers</summary>

1. True | 2. False | 3. False | 4. True | 5. False
6. B | 7. C | 8. B | 9. B | 10. B
11. A, C, E | 12. A, C | 13. A, C, E
14. C | 15. C | 16. B | 17. A | 18. B | 19. B | 20. B
21. B | 22. A | 23. True | 24. B | 25. B | 26. B | 27. B | 28. B
</details>

---

## 📜 Domain 2: Vault Policies (Questions 29-52)

### True/False
29. True or False: An empty policy grants no permissions (deny by default).

30. True or False: The `*` wildcard can be used anywhere in a policy path.

31. True or False: The `default` policy can be completely removed from Vault.

32. True or False: The `sudo` capability is required to access protected paths like `sys/policies`.

### Multiple Choice
33. Which capability allows reading secret values from a KV secrets engine?
    - A) `write`
    - B) `read`
    - C) `list`
    - D) `create`

34. What does the `+` wildcard match in a policy path like `secret/+/config`?
    - A) Any number of path segments
    - B) Exactly one path segment
    - C) Only numeric values
    - D) Paths ending with "config"

35. Which policy statement allows listing keys under `kv/data/apps/` but not reading them?
    ```hcl
    path "kv/data/apps/*" {
      capabilities = [?]
    }
    ```
    - A) `["read"]`
    - B) `["list"]`
    - C) `["read", "list"]`
    - D) `["sudo"]`

36. To deny access to a specific path while allowing parent paths, which capability takes precedence?
    - A) `read`
    - B) `deny`
    - C) `sudo`
    - D) `list`

37. Which command creates or updates a policy named `app-policy` from a file?
    - A) `vault policy create app-policy @policy.hcl`
    - B) `vault policy write app-policy @policy.hcl`
    - C) `vault write sys/policy/app-policy @policy.hcl`
    - D) `vault policy apply app-policy policy.hcl`

### Multiple Answer (Choose 2)
38. Which capabilities are valid in Vault policies? (Choose 2)
    - A) `create`
    - B) `modify`
    - C) `delete`
    - D) `edit`
    - E) `sudo`

39. Which paths require `sudo` capability to access? (Choose 2)
    - A) `kv/data/app/config`
    - B) `sys/policies/acl`
    - C) `auth/token/create-orphan`
    - D) `secret/data/foo`
    - E) `identity/entity/name`

40. What are true about policy inheritance? (Choose 2)
    - A) Child tokens inherit parent token policies unless orphaned
    - B) Policies automatically cascade to all sub-paths
    - C) Identity policies combine with token policies
    - D) The `default` policy overrides all others
    - E) Most specific path rule wins

### Scenario-Based
41. You need a policy that allows an app to read secrets under `kv/data/payment/*` but NOT `kv/data/payment/admin/*`. Which is CORRECT?

42. Which policy allows a service to renew its own token but not create new tokens?

43. To allow listing secret names but not reading their values, which capability do you use?
    - A) `read`
    - B) `list`
    - C) `sudo`
    - D) `create`

44. What does this policy path match? `path "kv/metadata/+/team_*"`
    - A) `kv/metadata/team_alpha` only
    - B) `kv/metadata/us-west/team_alpha`
    - C) `kv/metadata/team/alpha`
    - D) `kv/metadata/team_alpha/config`

45. Which command tests what capabilities a token has on a path?
    - A) `vault token verify $TOKEN $PATH`
    - B) `vault token capabilities $TOKEN $PATH`
    - C) `vault policy check $TOKEN $PATH`
    - D) `vault auth test $PATH`

46. You want to create a policy using identity templating to allow users to access their own secrets. Which path syntax is correct?

47. What happens if a token has both `read` and `deny` capabilities for the same path?
    - A) `read` wins (first match)
    - B) `deny` wins (most restrictive)
    - C) Error: conflicting capabilities
    - D) Depends on policy order

48. Which policy allows creating but not reading secrets?

49. To grant access to manage auth methods, which path/capability combination is needed?
    - A) `path "auth/*" { capabilities = ["read"] }`
    - B) `path "sys/auth/*" { capabilities = ["create", "update", "delete", "sudo"] }`
    - C) `path "sys/mounts/*" { capabilities = ["list"] }`
    - D) `path "identity/*" { capabilities = ["sudo"] }`

50. Which statement about the `default` policy is TRUE?
    - A) It can be deleted to restrict all access
    - B) It grants `read` access to all paths by default
    - C) It provides basic token self-lookup and cubbyhole access
    - D) It overrides all custom policies

51. What capability is needed to delete a specific version of a KV v2 secret?
    - A) `delete` on `kv/data/...`
    - B) `delete` on `kv/metadata/...`
    - C) `update` on `kv/delete/...`
    - D) `sudo` on `kv/...`

52. Which policy allows an app to read its own cubbyhole?

<details>
<summary>Show Answers</summary>

29. True | 30. False | 31. False | 32. True
33. B | 34. B | 35. B | 36. B | 37. B
38. A, C, E | 39. B, C | 40. A, C, E
41. Use `deny` on admin path | 42. `update` on `auth/token/renew-self` | 43. B
44. B | 45. B | 46. `{{identity.entity.name}}` | 47. B | 48. `["create"]`
49. B | 50. C | 51. C | 52. `path "cubbyhole/*" { capabilities = ["read","list"] }`
</details>

---

## 🎫 Domain 3: Vault Tokens (Questions 53-76)

### True/False
53. True or False: Batch tokens can be renewed indefinitely.

54. True or False: Orphan tokens expire when their parent token expires.

55. True or False: Root tokens should be used for daily administrative tasks.

56. True or False: Token accessors can be used to look up token properties without exposing the token ID.

### Multiple Choice
57. Which token type starts with `hvs.` in Vault 1.10+?
    - A) Batch token
    - B) Service token
    - C) Recovery token
    - D) Root token

58. What does `explicit_max_ttl` do when set on a token?
    - A) Extends the token's renewable period indefinitely
    - B) Sets an absolute maximum TTL that cannot be extended by renewal
    - C) Disables token renewal
    - D) Makes the token orphaned

59. Which command creates a token with a 1-hour TTL?
    - A) `vault token create --ttl=1h`
    - B) `vault token generate -time 60m`
    - C) `vault create token -duration 3600`
    - D) `vault token new -ttl 60`

60. What is the primary use case for periodic tokens?
    - A) One-time secret delivery
    - B) Long-running services that need indefinite access with periodic renewal
    - C) Temporary human user access
    - D) Backup authentication

61. Which token property indicates how many times a token can be used?
    - A) `ttl`
    - B) `max_ttl`
    - C) `num_uses`
    - D) `use_limit`

### Multiple Answer (Choose 2)
62. Which are characteristics of batch tokens? (Choose 2)
    - A) Lightweight, no storage overhead
    - B) Can create child tokens
    - C) Cannot be renewed
    - D) Support accessors
    - E) Have fixed lifetime (no renewal)

63. What actions can be performed using a token accessor? (Choose 2)
    - A) Look up token properties
    - B) Create child tokens
    - C) Revoke the token
    - D) Renew with extended TTL beyond max
    - E) Renew the token

64. Which statements about token hierarchy are true? (Choose 2)
    - A) Child tokens expire when parent is revoked
    - B) All tokens must have a parent
    - C) Orphan tokens break the parent-child expiration chain
    - D) Root tokens cannot have children
    - E) Service tokens can create child tokens

### Scenario-Based
65. An application needs a token that survives parent token revocation. Which token type should you create?
    - A) Batch token
    - B) Orphan token
    - C) Root token
    - D) Periodic token

66. You need to audit which token accessed a secret, but don't want to expose the token ID. What do you use?
    - A) Token ID
    - B) Token accessor
    - C) Entity ID
    - D) Lease ID

67. Which command revokes a token using its accessor?
    - A) `vault token revoke -accessor <accessor>`
    - B) `vault token delete --by-accessor <accessor>`
    - C) `vault revoke token <accessor>`
    - D) `vault token invalidate <accessor>`

68. A token has `ttl=30m` and `max_ttl=2h`. After 30 minutes, you renew it with `+1h`. What is the new expiration?
    - A) 30 minutes from renewal
    - B) 1 hour from renewal
    - C) 2 hours from original creation (max_ttl limit)
    - D) Indefinite

69. Which token type is BEST for a CI/CD job that runs every 6 hours and needs Vault access for 10 minutes?
    - A) Root token
    - B) Periodic token with 24h period
    - C) Service token with 15m TTL, renewed by auto-auth
    - D) Batch token with 10m TTL

70. What happens when you revoke a token that has active leases?
    - A) Leases continue until their TTL expires
    - B) All leases created by that token are also revoked
    - C) Leases must be manually revoked first
    - D) Only dynamic secret leases are revoked

71. Which command shows detailed information about the current token?
    - A) `vault token info`
    - B) `vault token lookup`
    - C) `vault read auth/token/lookup-self`
    - D) Both B and C

72. To create a token that cannot create child tokens, which parameter do you use?
    - A) `--no-children`
    - B) `--type=batch`
    - C) `--orphan=false`
    - D) `--limit-children=0`

73. What does `renewable=false` in token lookup output indicate?
    - A) The token has expired
    - B) The token cannot be renewed (e.g., batch token or max_ttl reached)
    - C) The token is orphaned
    - D) Renewal requires sudo capability

74. Which token type is automatically created when authenticating via any auth method?
    - A) Root token
    - B) Service token (by default)
    - C) Batch token
    - D) Orphan token

75. You need a token for emergency access that bypasses normal policy restrictions. Which do you use?
    - A) Token with `sudo` capability on all paths
    - B) Root token
    - C) Token with `deny` overridden
    - D) Token with `*` wildcard policy

76. What is the default `default_lease_ttl` for tokens if not configured?
    - A) 1 hour
    - B) 32 days (768 hours)
    - C) 7 days
    - D) Unlimited

<details>
<summary>Show Answers</summary>

53. False | 54. False | 55. False | 56. True
57. B | 58. B | 59. A | 60. B | 61. C
62. A, C, E | 63. A, C, E | 64. A, C, E
65. B | 66. B | 67. A | 68. C | 69. C | 70. B
71. D | 72. B | 73. B | 74. B | 75. B | 76. B
</details>

---

## 🔄 Domain 4: Vault Leases (Questions 77-92)

### True/False
77. True or False: All secrets in Vault have leases.

78. True or False: When a lease expires, the associated secret is automatically revoked at the backend.

79. True or False: The `renew` command extends a lease from its original start time.

### Multiple Choice
80. Which command renews a lease for an additional 30 minutes?
    - A) `vault lease renew -increment=30m <lease_id>`
    - B) `vault lease extend <lease_id> +30m`
    - C) `vault renew <lease_id> --ttl 1800`
    - D) `vault lease update <lease_id> -time 30m`

81. What does `lease_renewable=true` indicate?
    - A) The lease can be renewed up to its max_ttl
    - B) The lease auto-renews indefinitely
    - C) Renewal requires root token
    - D) The lease is for a static secret

82. Which path is used to look up lease information?
    - A) `sys/leases/lookup`
    - B) `sys/lease/info`
    - C) `vault/leases/read`
    - D) `auth/lease/status`

83. To revoke all leases under a path prefix, which command do you use?
    - A) `vault lease revoke -prefix <path>/`
    - B) `vault lease delete <path>/*`
    - C) `vault revoke --recursive <path>/`
    - D) `vault lease purge <path>/`

### Multiple Answer (Choose 2)
84. Which lease operations require the `lease_id` as an argument? (Choose 2)
    - A) `renew`
    - B) `revoke -prefix`
    - C) `create`
    - D) `list`
    - E) `revoke` (specific lease)

85. What metadata is included in a lease? (Choose 2)
    - A) `lease_duration`
    - B) `policy_name`
    - C) `lease_id`
    - D) `token_accessor`
    - E) `lease_renewable`

### Scenario-Based
86. A database credential lease expires in 10 minutes. The app needs it for 2 more hours. What should it do?
    - A) Request a new credential
    - B) Renew the lease with sufficient increment
    - C) Use a static secret instead
    - D) Increase the system max_ttl

87. You run `vault lease revoke -force -prefix database/`. What happens?
    - A) All database leases are renewed
    - B) Leases are revoked from Vault even if backend revocation fails
    - C) Only valid leases are revoked
    - D) The database plugin is disabled

88. Which inheritance level has the HIGHEST priority for lease TTL settings?
    - A) System default
    - B) Mount configuration
    - C) Object/lease-specific setting
    - D) Policy setting

89. What happens to leases when their associated token is revoked?
    - A) Leases continue until TTL
    - B) Leases are automatically revoked
    - C) Leases become orphaned
    - D) Only renewable leases are revoked

90. Which command lists active leases for a specific path?
    - A) `vault list sys/leases/lookup/<path>`
    - B) `vault lease list <path>`
    - C) `vault read sys/leases/<path>`
    - D) `vault secrets leases <path>`

91. A lease has `lease_duration=1h` and `max_ttl=4h`. After 30 minutes, you renew with `+2h`. What's the new duration?
    - A) 2 hours from renewal
    - B) 2.5 hours total from original start
    - C) Up to 4 hours from original creation (max_ttl limit)
    - D) Unlimited

92. Why might `vault lease renew` fail with "lease not found"?
    - A) The lease already expired or was revoked
    - B) The token lacks `sudo` capability
    - C) The increment value is too large
    - D) The lease is for a static secret

<details>
<summary>Show Answers</summary>

77. False | 78. True | 79. False
80. A | 81. A | 82. A | 83. A
84. A, E | 85. A, C, E
86. B | 87. B | 88. C | 89. B | 90. A | 91. C | 92. A
</details>

---

## 🔑 Domain 5: Secrets Engines (Questions 93-124)

### True/False
93. True or False: The KV secrets engine v2 supports secret versioning by default.

94. True or False: Disabling a secrets engine permanently deletes all its data.

95. True or False: The Transit secrets engine stores the encrypted data it processes.

96. True or False: Dynamic secrets are automatically revoked after their TTL expires.

### Multiple Choice
97. Which secrets engine is designed for encryption-as-a-service without storing data?
    - A) KV v2
    - B) Database
    - C) Transit
    - D) PKI

98. To enable KV v2 at a custom path `apps/secrets`, which command do you use?
    - A) `vault secrets enable -path=apps/secrets -version=2 kv`
    - B) `vault enable kv -v2 -p apps/secrets`
    - C) `vault secrets add kv-v2 apps/secrets`
    - D) `vault mount -version=2 kv apps/secrets`

99. What is the primary benefit of dynamic secrets from the Database secrets engine?
    - A) Faster secret retrieval
    - B) Automatic credential rotation and revocation
    - C) Encryption of stored credentials
    - D) Version history of secrets

100. Which command generates a new encryption key version in the Transit engine?
    - A) `vault write transit/keys/mykey/rotate -force`
    - B) `vault transit rotate mykey`
    - C) `vault write transit/rotate mykey`
    - D) `vault keys rotate transit/mykey`

101. To read a specific version of a KV v2 secret, which syntax is correct?
    - A) `vault kv get -version=3 secret/foo`
    - B) `vault read secret/foo?version=3`
    - C) `vault kv get secret/foo@3`
    - D) `vault read -version 3 secret/data/foo`

### Multiple Answer (Choose 2)
102. Which are characteristics of dynamic secrets? (Choose 2)
    - A) Generated on-demand with unique credentials
    - B) Stored permanently in Vault storage
    - C) Automatically revoked after TTL
    - D) Support unlimited version history
    - E) Reduce risk of credential leakage

103. Which secrets engines support dynamic secret generation? (Choose 2)
    - A) KV v1
    - B) Database
    - C) Cubbyhole
    - D) AWS
    - E) Transit

104. What can you do with the Transit secrets engine? (Choose 2)
    - A) Encrypt/decrypt data
    - B) Store long-term secrets
    - C) Rotate encryption keys
    - D) Generate database credentials
    - E) Sign/verify data (with asymmetric keys)

### Scenario-Based
105. Your app needs to encrypt sensitive data before storing it in a database. Which secrets engine should you use?
    - A) KV v2
    - B) Transit
    - C) Database
    - D) PKI

106. You need to issue short-lived database credentials that auto-expire. Which secrets engine?
    - A) KV v2
    - B) Transit
    - C) Database
    - D) SSH

107. To enable the PKI secrets engine and configure it to issue certificates with max 1-year TTL, which command do you use?

108. Which path do you use to encrypt data with the Transit engine?
    - A) `transit/encrypt/<key_name>`
    - B) `transit/keys/<key_name>/encrypt`
    - C) `encrypt/transit/<key_name>`
    - D) `transit/data/encrypt/<key_name>`

109. What does the `cubbyhole` secrets engine provide?
    - A) Shared secret storage for teams
    - B) Private, token-scoped secret storage
    - C) Encrypted backup storage
    - D) Temporary dynamic credentials

110. To list all enabled secrets engines, which command?
    - A) `vault secrets list`
    - B) `vault list secrets/`
    - C) `vault read sys/mounts`
    - D) Both A and C

111. Which parameter moves a secrets engine to a new path?
    - A) `vault secrets move <old> <new>`
    - B) `vault mount -move <old> <new>`
    - C) `vault secrets rename <old> <new>`
    - D) `vault path move <old> <new>`

112. What happens to existing leases when you move a secrets engine?
    - A) Leases continue normally
    - B) All leases are revoked
    - C) Leases are migrated automatically
    - D) Only dynamic secret leases are revoked

113. To configure a database secrets engine connection, which command structure is correct?

114. Which capability is needed to read a secret from KV v2?
    - A) `read` on `kv/data/<path>`
    - B) `read` on `kv/<path>`
    - C) `list` on `kv/metadata/<path>`
    - D) `sudo` on `kv/*`

115. To destroy a specific version of a KV v2 secret:
    - A) `vault kv destroy -version=2 secret/foo`
    - B) `vault kv delete -version=2 secret/foo`
    - C) `vault delete kv/data/foo?version=2`
    - D) `vault kv metadata delete secret/foo`

116. What is the difference between `kv delete` and `kv metadata delete`?
    - A) `delete` soft-deletes (can undelete); `metadata delete` permanently removes
    - B) `delete` removes all versions; `metadata delete` removes one version
    - C) No difference
    - D) `metadata delete` requires sudo

117. Which secrets engine would you use to generate SSH certificates for server access?
    - A) SSH secrets engine
    - B) Transit
    - C) PKI
    - D) KV

118. To enable response wrapping for a secret, which approach?
    - A) Use `vault token create -wrap-ttl=5m` and write secret to cubbyhole
    - B) Set `wrap=true` on secret write
    - C) Use Transit engine encryption
    - D) Enable PKI with short TTL

119. What does `vault kv patch` do?
    - A) Deletes a secret
    - B) Updates only specified fields without overwriting entire secret
    - C) Restores a deleted version
    - D) Encrypts the secret

120. Which command restores a previous version of a KV v2 secret as current?
    - A) `vault kv rollback -version=2 secret/foo`
    - B) `vault kv restore secret/foo@2`
    - C) `vault kv undelete -version=2 secret/foo`
    - D) `vault kv revert secret/foo 2`

121. To configure automatic key rotation in Transit engine (Vault 1.10+):
    - A) `vault write transit/keys/mykey/config auto_rotate_period=720h`
    - B) `vault transit rotate --auto mykey`
    - C) Set `rotation_schedule` in mount config
    - D) Use cron job with `vault write -force transit/keys/mykey/rotate`

122. Which secrets engine supports TOTP (time-based one-time passwords)?
    - A) TOTP secrets engine
    - B) Transit
    - C) PKI
    - D) KV

123. To generate a TOTP code for MFA, which command?

124. What is the purpose of the `cas` parameter in KV v2 writes?
    - A) Encrypt the secret
    - B) Check-and-set for optimistic concurrency control
    - C) Compress the secret data
    - D) Create an audit log entry

<details>
<summary>Show Answers</summary>

93. True | 94. True | 95. False | 96. True
97. C | 98. A | 99. B | 100. A | 101. A
102. A, C, E | 103. B, D | 104. A, C, E
105. B | 106. C | 107. `vault secrets tune -max-lease-ttl=8760h pki`
108. A | 109. B | 110. D | 111. A | 112. B
113. `vault write database/config/...` with plugin_name, connection_url, etc.
114. A | 115. A | 116. A | 117. A | 118. A | 119. B | 120. A
121. A | 122. A | 123. `vault read totp/code/mykey` | 124. B
</details>

---

## 🔐 Domain 6: Encryption as a Service (Questions 125-136)

### True/False
125. True or False: The Transit engine can use both symmetric and asymmetric encryption algorithms.

126. True or False: Rotating a Transit key automatically re-encrypts existing data.

### Multiple Choice
127. Which command decrypts data using the Transit engine?
    - A) `vault write transit/decrypt/mykey ciphertext=<value>`
    - B) `vault transit decrypt mykey --data <value>`
    - C) `vault read transit/decrypt/mykey?ciphertext=<value>`
    - D) `vault decrypt transit/mykey <value>`

128. What format does Transit engine ciphertext use?
    - A) Plain base64
    - B) `vault:v1:<base64>`
    - C) JSON with metadata
    - D) Hex-encoded

129. To rewrap ciphertext with the latest key version:
    - A) `vault write transit/rewrap/mykey ciphertext=<old_ciphertext>`
    - B) `vault transit rewrap mykey <ciphertext>`
    - C) Decrypt then re-encrypt manually
    - D) `vault write transit/rotate mykey --rewrap`

### Multiple Answer (Choose 2)
130. Which operations does the Transit engine support? (Choose 2)
    - A) Encrypt/Decrypt
    - B) Store secrets long-term
    - C) HMAC/Verify
    - D) Generate database credentials
    - E) Generate data keys

131. What are benefits of key rotation in Transit? (Choose 2)
    - A) Limits exposure if a key is compromised
    - B) Reduces storage requirements
    - C) Compliance with key lifecycle policies
    - D) Automatically updates all encrypted data
    - E) Enables cryptographic agility

### Scenario-Based
132. Your app encrypts data with Transit key v1. After rotating to v2, how do you decrypt old data?
    - A) Manually re-encrypt with v2
    - B) Transit automatically uses correct key version based on ciphertext metadata
    - C) Store key versions separately
    - D) Use `min_decryption_version` to force v2

133. To set minimum decryption version to 3 (archive older keys):
    - A) `vault write transit/keys/mykey/config min_decryption_version=3`
    - B) `vault transit config mykey --min-decrypt 3`
    - C) Set in mount configuration
    - D) `vault write transit/keys/mykey/archive --before 3`

134. Which parameter specifies the encryption algorithm when creating a Transit key?
    - A) `vault write -force transit/keys/mykey type="aes256-gcm96"`
    - B) `--algorithm=aes256` in enable command
    - C) Set in policy
    - D) Configured at mount level

135. What does `min_encryption_version` control?
    - A) Oldest key version allowed for encryption
    - B) Minimum key length
    - C) Required Vault version
    - D) Minimum TTL for encrypted data

136. To generate a data key (for client-side encryption), which command?

<details>
<summary>Show Answers</summary>

125. True | 126. False
127. A | 128. B | 129. A
130. A, C, E | 131. A, C, E
132. B | 133. A | 134. A | 135. A | 136. `vault write transit/datakey/...`
</details>

---

## 🏗️ Domain 7: Architecture Fundamentals (Questions 137-156)

### True/False
137. True or False: Vault encrypts data before writing to the storage backend.

138. True or False: The storage backend can read plaintext secrets.

139. True or False: Sealing Vault encrypts the master key with unseal keys.

### Multiple Choice
140. What is the purpose of Shamir secret sharing?
    - A) Encrypt data at rest
    - B) Split the master key into shares for distributed unsealing
    - C) Authenticate users
    - D) Rotate encryption keys

141. Which environment variable sets the Vault server address for CLI?
    - A) `VAULT_SERVER`
    - B) `VAULT_ADDR`
    - C) `VAULT_API`
    - D) `VAULT_ENDPOINT`

142. What does `vault operator init` do?
    - A) Starts the Vault server
    - B) Initializes Vault, generating unseal keys and root token
    - C) Enables the KV secrets engine
    - D) Configures storage backend

143. To unseal Vault manually, how many unseal keys are typically required?
    - A) 1
    - B) 3 (default threshold)
    - C) All shares
    - D) Depends on token TTL

### Multiple Answer (Choose 2)
144. Which components are part of Vault's core architecture? (Choose 2)
    - A) Cryptographic barrier
    - B) Load balancer
    - C) Storage backend
    - D) API gateway
    - E) Auth methods

145. What happens during Vault sealing? (Choose 2)
    - A) Master key is encrypted and removed from memory
    - B) All tokens are revoked
    - C) Vault cannot decrypt data until unsealed
    - D) Storage backend is wiped
    - E) Unseal keys are required to recover master key

### Scenario-Based
146. Your Vault is sealed after a restart. What must you do before it can serve requests?
    - A) Re-initialize Vault
    - B) Provide sufficient unseal keys to reconstruct the master key
    - C) Restore from snapshot
    - D) Re-enable auth methods

147. Which command checks if Vault is sealed?
    - A) `vault status`
    - B) `vault health`
    - C) `vault server -check`
    - D) `vault read sys/seal-status`

148. To configure auto-unseal with AWS KMS, which config stanza is correct?

149. What is the difference between seal and recovery keys?
    - A) No difference
    - B) Seal keys unseal Vault; recovery keys are for auto-unseal configurations
    - C) Recovery keys are for backup only
    - D) Seal keys are for Enterprise only

150. Which command rotates the encryption key used for barrier encryption?
    - A) `vault operator rotate`
    - B) `vault seal rotate`
    - C) `vault operator rekey`
    - D) `vault keys rotate`

151. What does `vault operator rekey` do?
    - A) Rotates the barrier encryption key
    - B) Generates new unseal/recovery keys (changes key shares)
    - C) Revokes all tokens
    - D) Re-initializes Vault

152. To set `VAULT_TOKEN` for CLI authentication:

153. Which file stores the current token for CLI sessions by default?
    - A) `~/.vault/config`
    - B) `~/.vault-token`
    - C) `/etc/vault/token`
    - D) `$VAULT_DATA/token`

154. What is the cryptographic barrier's role?
    - A) Authenticates users
    - B) Encrypts/decrypts data before storage backend access
    - C) Manages policies
    - D) Handles audit logging

155. True or False: The storage backend must be trusted in Vault's security model.

156. Which command starts Vault in dev mode (in-memory, auto-unsealed)?
    - A) `vault server -dev`
    - B) `vault start --dev`
    - C) `vault run -mode=dev`
    - D) `vault init -dev`

<details>
<summary>Show Answers</summary>

137. True | 138. False | 139. True
140. B | 141. B | 142. B | 143. B
144. A, C, E | 145. A, C, E
146. B | 147. A | 148. `seal "awskms" {...}` | 149. B | 150. A
151. B | 152. `export VAULT_TOKEN="s.xxxx"` | 153. B | 154. B | 155. False | 156. A
</details>

---

## 🌐 Domain 8: Deployment Architecture (Questions 157-180)

### True/False
157. True or False: Raft storage backend supports high availability natively.

158. True or False: Performance replication secondaries can serve write requests.

159. True or False: Disaster Recovery promotion is automatic.

### Multiple Choice
160. Which storage backend is recommended for production HA deployments?
    - A) File
    - B) In-memory
    - C) Raft (integrated storage)
    - D) Etcd

161. What port does Vault use for cluster communication?
    - A) 8200 (API)
    - B) 8201 (cluster)
    - C) 8300 (gossip)
    - D) 443 (HTTPS)

162. To join a Raft cluster as a new node:
    - A) `vault operator raft join https://leader:8200`
    - B) `vault server -join https://leader:8200`
    - C) Configure `retry_join` in config file only
    - D) `vault cluster add https://leader:8200`

163. Which command lists Raft cluster peers?
    - A) `vault operator raft list-peers`
    - B) `vault cluster status`
    - C) `vault read sys/storage/raft/peers`
    - D) `vault status -cluster`

### Multiple Answer (Choose 2)
164. Which are valid storage backends officially supported by HashiCorp? (Choose 2)
    - A) Raft (integrated)
    - B) MongoDB
    - C) Consul
    - D) Redis
    - E) File (for testing)

165. What are characteristics of Performance Replication? (Choose 2)
    - A) Secondaries serve read requests locally
    - B) Secondaries can process writes independently
    - C) Policies and auth methods replicated from primary
    - D) Tokens are globally shared
    - E) Local tokens/leases on secondaries

166. Disaster Recovery replication features: (Choose 2)
    - A) Full cluster state replication
    - B) Secondaries serve client requests normally
    - C) Manual promotion to primary required
    - D) Automatic failover
    - E) Secondary data overwritten on promotion

### Scenario-Based
167. Your organization needs multi-region read scalability. Which replication type?
    - A) Disaster Recovery
    - B) Performance Replication
    - C) No replication; use load balancer
    - D) Raft across regions

168. For a DR site that takes over only during catastrophic failure, which replication?
    - A) Performance Replication
    - B) Disaster Replication
    - C) Raft multi-region
    - D) Storage backend replication

169. To configure Raft storage in `vault.hcl`, which stanza is correct?

170. What does `cluster_addr` configure?
    - A) Client API address
    - B) Address for inter-node cluster communication
    - C) UI endpoint
    - D) Audit log destination

171. Which command initiates Vault initialization with custom Shamir parameters?
    - A) `vault operator init -key-shares=5 -key-threshold=3`
    - B) `vault init --shares 5 --threshold 3`
    - C) Configure in `vault.hcl` only
    - D) `vault seal init -s 5 -t 3`

172. To generate a root token using unseal keys (emergency):

173. What is the purpose of `api_addr` in config?
    - A) Cluster communication
    - B) Address clients should use to reach this node
    - C) Internal service mesh address
    - D) Audit log endpoint

174. Which feature requires Vault Enterprise?
    - A) Raft storage
    - B) Namespaces
    - C) KV secrets engine
    - D) Token auth

175. To enable auto-unseal with Transit engine from another Vault, which config is correct?

176. What happens to a Raft node that loses quorum?
    - A) Automatically rejoins
    - B) Becomes standby; cannot serve writes until quorum restored
    - C) Data is lost
    - D) Requires re-initialization

177. Which command takes a Raft snapshot for backup?
    - A) `vault operator raft snapshot save backup.snap`
    - B) `vault backup create backup.snap`
    - C) `vault raft snapshot --output backup.snap`
    - D) `vault operator backup save backup.snap`

178. To restore from a Raft snapshot:
    - A) `vault operator raft snapshot restore backup.snap`
    - B) `vault restore backup.snap`
    - C) Replace data directory and restart
    - D) `vault operator init --restore backup.snap`

179. What is request forwarding in Vault clustering?
    - A) Load balancer distributes requests
    - B) Standby nodes forward write requests to active leader transparently
    - C) Clients retry failed requests
    - D) Replication of write operations

180. Which storage backend does NOT support HA?
    - A) Raft
    - B) Consul
    - C) File
    - D) All support HA

<details>
<summary>Show Answers</summary>

157. True | 158. False | 159. False
160. C | 161. B | 162. A | 163. A
164. A, C, E | 165. A, C, E | 166. A, C, E
167. B | 168. B | 169. `storage "raft" { path=... node_id=... retry_join {...} }`
170. B | 171. A | 172. `vault operator generate-root` workflow
173. B | 174. B | 175. `seal "transit" {...}` config
176. B | 177. A | 178. A | 179. B | 180. C
</details>

---

## 🤖 Domain 9: Access Management Architecture (Questions 181-200)

### True/False
181. True or False: Vault Agent can auto-renew tokens on behalf of applications.

182. True or False: Vault Secrets Operator is required for Kubernetes integrations.

### Multiple Choice
183. What is the primary purpose of Vault Agent?
    - A) Replace the Vault server
    - B) Auto-authenticate and manage tokens for applications
    - C) Encrypt data at rest
    - D) Audit all Vault requests

184. Which config section in `agent.hcl` defines authentication?
    - A) `auth`
    - B) `auto_auth`
    - C) `authentication`
    - D) `login`

185. To cache tokens in Vault Agent for client use, which config block is correct?

### Multiple Answer (Choose 2)
186. Vault Agent capabilities: (Choose 2)
    - A) Auto-authentication with configured method
    - B) Replace Vault server for storage
    - C) Token renewal and caching
    - D) Encrypt secrets at rest
    - E) Template rendering for secrets

187. Vault Secrets Operator features: (Choose 2)
    - A) Sync Vault secrets to Kubernetes Secrets
    - B) Replace Vault auth methods
    - C) Automatic secret rotation in K8s
    - D) Manage Vault storage backend
    - E) Support for KV, PKI, Database engines

### Scenario-Based
188. Your Kubernetes app needs Vault secrets without embedding tokens. Which solution?
    - A) Static tokens in environment variables
    - B) Vault Agent sidecar with Kubernetes auth
    - C) Manual token distribution
    - D) Disable authentication

189. Which command starts Vault Agent with a config file?
    - A) `vault agent -config=agent.hcl`
    - B) `vault run agent --config agent.hcl`
    - C) `vault agent start -f agent.hcl`
    - D) `vault server -agent -config agent.hcl`

190. What does the `template` block in Vault Agent config do?
    - A) Defines auth method parameters
    - B) Renders secrets into files using Go templates
    - C) Sets token TTL values
    - D) Configures audit logging

191. Which annotation enables Vault Agent injection in Kubernetes?
    - A) `vault.hashicorp.com/agent-inject: "true"`
    - B) `vault.io/inject-agent: enabled`
    - C) `kubernetes.vault.com/agent: true`
    - D) `vault.agent.inject: "on"`

192. To configure Vault Agent to write a secret to a file:
    ```hcl
    template {
      destination = "?"
      contents = "?"
    }
    ```
    What goes in `destination`?

193. What is the purpose of `exit_after_auth` in Vault Agent config?
    - A) Stops Agent after first authentication
    - B) Exits if auth fails
    - C) Runs Agent as one-time auth helper
    - D) Disables token renewal

194. Which command verifies Vault Agent is running and authenticated?
    - A) `vault agent status`
    - B) `curl http://127.0.0.1:8100/v1/sys/health`
    - C) `vault token lookup`
    - D) `vault agent health-check`

195. What does Vault Secrets Operator sync to Kubernetes?
    - A) Vault policies
    - B) Vault secrets as Kubernetes Secrets
    - C) Vault tokens as ServiceAccounts
    - D) Vault config as ConfigMaps

196. Which CRD does Vault Secrets Operator use to define secret sync?
    - A) `VaultSecret`
    - B) `SecretSync`
    - C) `VaultIntegration`
    - D) `KubernetesVault`

197. To configure auto-auth method in Vault Agent for Kubernetes:
    ```hcl
    method "kubernetes" {
      mount_path = "?"
      config = {
        role = "?"
      }
    }
    ```
    What is `mount_path` typically set to?

198. What happens if Vault Agent cannot reach the Vault server?
    - A) Agent crashes immediately
    - B) Agent retries based on backoff configuration
    - C) Agent switches to offline mode
    - D) Agent writes secrets to local cache only

199. Which environment variable overrides Vault Agent config file path?
    - A) `VAULT_AGENT_CONFIG`
    - B) `VAULT_CONFIG`
    - C) `AGENT_CONFIG_PATH`
    - D) `VAULT_AGENT_FILE`

200. To disable Vault Agent caching:
    - A) Set `cache { enabled = false }`
    - B) Remove the `cache` block entirely
    - C) Set `use_auto_auth_token = false`
    - D) Both A and B

<details>
<summary>Show Answers</summary>

181. True | 182. False
183. B | 184. B | 185. `cache { use_auto_auth_token=true }` + `listener` block
186. A, C, E | 187. A, C, E
188. B | 189. A | 190. B | 191. A | 192. File path like `/vault/secrets/db-creds`
193. C | 194. B | 195. B | 196. A | 197. `auth/kubernetes` | 198. B
199. A | 200. D
</details>

---

## 🔑 Answer Key (Check After Completing All Questions)

<details>
<summary>▶️ Click to Expand Answer Key</summary>

### Domain 1: Authentication Methods
1. True | 2. False | 3. False | 4. True | 5. False
6. B | 7. C | 8. B | 9. B | 10. B
11. A, C, E | 12. A, C | 13. A, C, E
14. C | 15. C | 16. B | 17. A | 18. B | 19. B | 20. B
21. B | 22. A | 23. True | 24. B | 25. B | 26. B | 27. B | 28. B

### Domain 2: Vault Policies
29. True | 30. False | 31. False | 32. True
33. B | 34. B | 35. B | 36. B | 37. B
38. A, C, E | 39. B, C | 40. A, C, E
41. Use `deny` on admin path | 42. `update` on `auth/token/renew-self` | 43. B
44. B | 45. B | 46. `{{identity.entity.name}}` | 47. B | 48. `["create"]`
49. B | 50. C | 51. C | 52. `path "cubbyhole/*" { capabilities = ["read","list"] }`

### Domain 3: Vault Tokens
53. False | 54. False | 55. False | 56. True
57. B | 58. B | 59. A | 60. B | 61. C
62. A, C, E | 63. A, C, E | 64. A, C, E
65. B | 66. B | 67. A | 68. C | 69. C | 70. B
71. D | 72. B | 73. B | 74. B | 75. B | 76. B

### Domain 4: Vault Leases
77. False | 78. True | 79. False
80. A | 81. A | 82. A | 83. A
84. A, E | 85. A, C, E
86. B | 87. B | 88. C | 89. B | 90. A | 91. C | 92. A

### Domain 5: Secrets Engines
93. True | 94. True | 95. False | 96. True
97. C | 98. A | 99. B | 100. A | 101. A
102. A, C, E | 103. B, D | 104. A, C, E
105. B | 106. C | 107. `vault secrets tune -max-lease-ttl=8760h pki`
108. A | 109. B | 110. D | 111. A | 112. B
113. `vault write database/config/...` with plugin_name, connection_url, etc.
114. A | 115. A | 116. A | 117. A | 118. A | 119. B | 120. A
121. A | 122. A | 123. `vault read totp/code/mykey` | 124. B

### Domain 6: Encryption as a Service
125. True | 126. False
127. A | 128. B | 129. A
130. A, C, E | 131. A, C, E
132. B | 133. A | 134. A | 135. A | 136. `vault write transit/datakey/...`

### Domain 7: Architecture Fundamentals
137. True | 138. False | 139. True
140. B | 141. B | 142. B | 143. B
144. A, C, E | 145. A, C, E
146. B | 147. A | 148. `seal "awskms" {...}` | 149. B | 150. A
151. B | 152. `export VAULT_TOKEN="s.xxxx"` | 153. B | 154. B | 155. False | 156. A

### Domain 8: Deployment Architecture
157. True | 158. False | 159. False
160. C | 161. B | 162. A | 163. A
164. A, C, E | 165. A, C, E | 166. A, C, E
167. B | 168. B | 169. `storage "raft" { path=... node_id=... retry_join {...} }`
170. B | 171. A | 172. `vault operator generate-root` workflow
173. B | 174. B | 175. `seal "transit" {...}` config
176. B | 177. A | 178. A | 179. B | 180. C

### Domain 9: Access Management Architecture
181. True | 182. False
183. B | 184. B | 185. `cache { use_auto_auth_token=true }` + `listener` block
186. A, C, E | 187. A, C, E
188. B | 189. A | 190. B | 191. A | 192. File path like `/vault/secrets/db-creds`
193. C | 194. B | 195. B | 196. A | 197. `auth/kubernetes` | 198. B
199. A | 200. D

</details>

---

> 💡 **Pro Tip**: After checking answers, review any questions you missed by running `vault path-help <path>` in your homelab to explore the actual API behavior. Hands-on practice cements knowledge better than memorization!

Good luck with your Vault Associate exam prep! 🎯 Let me know if you'd like explanations for any specific questions.
